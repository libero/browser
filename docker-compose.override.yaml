version: '3'

services:
    composer:
        volumes:
            - ./:/app
            - vendor:/app/vendor
    fpm:
        volumes:
            - ./:/var/www/html/
            - /var/www/html/var
            - vendor:/var/www/html/vendor

volumes:
    vendor:
