parameters:
    env(DEFAULT_LOCALE): 'en'
    env(GTM_ID): null
    env(INFO_BAR): null

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    Libero\Browser\:
        resource: '../src/*'
        exclude: '../src/{Kernel.php}'
